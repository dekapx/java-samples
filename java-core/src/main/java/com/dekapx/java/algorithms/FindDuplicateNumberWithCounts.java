package com.dekapx.java.algorithms;

/**
 * Find the duplicate numbers with their counts
 * Declare an array of the size, by finding the highest number in existing array.
 * Iterate and fill the found number on index of the number
 * Increment the counter when find the number
 * input = {2,3,1,5,3,4}
 */
public class FindDuplicateNumberWithCounts {
    public static void main(String[] args) {

    }

    private static int[] getDuplicates(final int[] numbers) {
        return null;
    }

    private static int getMax(final int[] numbers) {
        return 0;
    }
}
